#include<stdio.h>
#include<string.h>

#define SIZE 100

int main(void)
{
    unsigned short i = 0;
    char expression[SIZE];
    double result;

    printf("Enter an expression: ");
    fgets(expression, SIZE, stdin);

    while(expression[i] != '\n'){

    }
}
